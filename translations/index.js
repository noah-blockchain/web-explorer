import en from './en.json'

// there is only common translations used across components
// if the translation is specific and used only in one component - add it to the component translations

export default { en }
